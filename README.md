# GoogleSearchResultScraper

This scraper consists of two services a .Net Core Web Api and a React App

The .Net Core Web Api is written in .Net 5. It has a swagger page and defaults to https://localhost:5001. It uses hexagonal architecture and mediatr to separate concerns.

The react app has a separate read me [here](./GoogleSearchResultScraperUI/README.md)
